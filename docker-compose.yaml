version: '3.8'

services:
  web:
    image: nginx:alpine
    ports:
      - '80:80'
    volumes:
      - ./:/app
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
    environment:
      - NGINX_HOST=techshaman.42024769.xyz
      - NGINX_PORT=80
    restart: unless-stopped
